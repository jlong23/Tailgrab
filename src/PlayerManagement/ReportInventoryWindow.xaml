<Window x:Class="Tailgrab.PlayerManagement.ReportInventoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Report Inventory"
        Width="850" Height="550"
        FontFamily="Segoe UI"
        Language="en-US"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="0"/>
        </Grid.ColumnDefinitions>

        <!-- Left side - Form fields -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- User ID -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <Label Content="User ID:" FontWeight="Bold"/>
                <TextBox Name="UserIdTextBox" TextChanged="OnInputFieldChanged"/>
            </StackPanel>

            <!-- Inventory ID -->
            <StackPanel Grid.Row="1" Margin="0,0,0,12">
                <Label Content="Inventory ID:" FontWeight="Bold"/>
                <TextBox Name="InventoryIdTextBox" TextChanged="OnInputFieldChanged"/>
            </StackPanel>

            <!-- Category -->
            <StackPanel Grid.Row="2" Margin="0,0,0,12">
                <Label Content="Category:" FontWeight="Bold"/>
                <TextBox Name="CategoryTextBox" IsReadOnly="True"/>
            </StackPanel>

            <!-- Report Type -->
            <StackPanel Grid.Row="3" Margin="0,0,0,12">
                <Label Content="Report Reason:" FontWeight="Bold"/>
                <ComboBox Name="ReportReasonComboBox" 
                          ItemsSource="{Binding ReportReasons}"
                          DisplayMemberPath="DisplayName"
                          SelectedValuePath="Value"
                          SelectedIndex="0"/>
            </StackPanel>

            <!-- Report Description -->
            <StackPanel Grid.Row="4" Margin="0,0,0,12">
                <Label Content="Report Description (AI Evaluation):" FontWeight="Bold"/>
                <TextBox Name="ReportDescriptionTextBox" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         IsReadOnly="True"
                         MinHeight="150"/>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="CancelButton" Content="Cancel" Width="100" Margin="0,0,12,0" Click="CancelButton_Click"/>
                <Button Name="SubmitButton" Content="Submit" Width="100" Click="SubmitButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Right side - Image display -->
        <Border Grid.Column="1" 
                BorderBrush="Gray" 
                BorderThickness="2" 
                VerticalAlignment="Top"
                Padding="8" Grid.ColumnSpan="2" Height="250" Width="230">
            <StackPanel>
                <Label Content="Image Preview:" FontWeight="Bold" HorizontalAlignment="Center"/>
                <Image Name="InventoryImagePreview" 
                       Width="200"
                       Height="200"
                       Stretch="None"
                       />
            </StackPanel>
        </Border>
    </Grid>
</Window>
